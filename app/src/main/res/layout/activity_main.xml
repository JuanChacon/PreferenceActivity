<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.tecii.android.preferenceactivity.MainActivity">

    <Button
        android:text="Carga la pantalla de preferencias"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="13dp"
        android:layout_marginTop="13dp"
        android:onClick="onClickLoad"
        android:id="@+id/buttonCar" />

    <Button
        android:text="Mostrar valores de las preferencias"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonCar"
        android:layout_alignStart="@+id/buttonCar"
        android:layout_marginTop="26dp"
        android:onClick="onClickDisplay"
        android:id="@+id/buttonMos" />

    <Button
        android:text="Modificar valores de preferencias"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignStart="@+id/buttonMos"
        android:onClick="onClickModify"
        android:id="@+id/buttonMod" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/editText"
        android:layout_above="@+id/buttonMod"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="18dp" />
</RelativeLayout>
